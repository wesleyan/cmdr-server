<div class="room-list">
  <ul class="buildings">
    {{#buildings}}
      <li>
        <a href="#" id="{{id}}">{{name}}</a>
        <ul class="rooms">
          {{#rooms}}
            <li><a href="#" id="{{id}}" data-search="{{../name}} {{params.name}}">{{params.name}}</a></li>
          {{/rooms}}
         </ul>
      </li>
    {{/buildings}}
  </ul>
</div>
